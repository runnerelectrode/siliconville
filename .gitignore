# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
game/node_modules/
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env
/.env.prod

# Vite build
dist

convex-local*
convex_local*

# Gatherville: upstream reference clones (not part of the build)
.aitown-upstream/
.genagents-upstream/
.tinytroupe-upstream/

# LimeZu tilesets are licensed for commercial use but NOT for redistribution.
# Ship them from a private bundle/CDN — never commit them.
assets/limezu/

# Local Convex backend. `convex env set` stores deployment env vars INSIDE this
# sqlite file, so it holds live API keys as well as all local twin data.
# Never commit it.
.convex/
